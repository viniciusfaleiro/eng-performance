# JPA slice tests run Flyway to build the schema on a real Postgres (Testcontainers),
# then validate the entity mappings against it.
spring.flyway.enabled=true
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false
