# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# IDE
.idea/
*.iml
.vscode/

# Node (not used — the UI is inline vanilla JS)
node_modules/

# OS
.DS_Store

# Logs / local
*.log

# Docker image staging (transient)
app.jar

# Segredos locais (CONFIG_ENCRYPTION_KEY e afins) lidos pelo docker compose
.env
